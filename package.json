{
  "name": "tourist-guide",
  "private": true,
  "version": "1.0.0",
  "description": "Root convenience scripts for the Tourist Guide monorepo (frontend/ + backend/).",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "bash -c \"cd backend && source venv/bin/activate && python manage.py runserver 8000\"",
    "build": "npm run build --prefix frontend",
    "install:frontend": "npm install --prefix frontend",
    "install:backend": "bash -c \"cd backend && python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
